{% extends 'base.html.twig' %}
{% block body %}
    <h1>Editar Contacto</h1>
<div class="container-fluid">
{{ form_start(form) }}
        {{ form_widget(form) }}
        {# <input type="submit" value="Create" /> #}
    {{ form_end(form) }}

    <a href="{{ path('View_all_posts') }}"><button name="button"class="btn btn-success">Regresar</button></a>
</div>   
{% endblock %}